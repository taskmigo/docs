---
title: Component
description: Define a typed, reusable UI template for Pages.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

A `v0/component` packages a component tree once so any Page can import and render it. Declare the values each Page may provide in `spec.props`, then read those values from `spec.template` through `{{ props.<name> }}`.

A Component may import other Components. Every reusable Component used in `spec.template` must appear in `imports`; built-in components such as `Stack` and `Typography` do not require imports.

A Component's `metadata.name` must not match the name of a built-in [UI Component](/versions/v0/manuals/ui-components). Built-in names are reserved, so `Stack` and `Typography` are invalid Component names.

```yaml
apiVersion: v0
kind: component

metadata:
  name: header

spec:
  props:
    title:
      type: string
      required: true
    showDescription:
      type: boolean
      required: false
      default: true
    items:
      type: array
      required: false
      default: []

  template:
    component: Stack
    children:
      - component: Typography
        props:
          variant: h1
        children: '{{ props.title }}'
```

## Fields

<TypeTable
  type={{
    'spec.props': {
      type: 'Dictionary<PropDefinition>',
      required: true,
      description: 'Maps each user-defined prop name to the definition fields listed below.',
    },
    'spec.props.*.type': {
      type: '"string" | "number" | "boolean" | "array"',
      required: true,
      description: 'Checks values passed by an importing Page.',
    },
    'spec.props.*.required': {
      type: 'boolean',
      required: true,
      description: 'Controls whether an importing Page may omit this prop.',
    },
    'spec.props.*.default': {
      type: 'string | number | boolean | (string | number | boolean)[]',
      required: false,
      description: 'Fallback used when an importing Page omits this prop.',
    },
    'spec.template': {
      type: 'ComponentNode',
      typeDescriptionLink: '/versions/v0/manuals/manifests/v0-page#component-nodes',
      required: true,
      description: 'Component tree rendered for every use, using the structure defined by Page component nodes.',
    },
  }}
/>

Prop names are case-sensitive. A `default` value must match its declared `type`. An `array` may contain strings, numbers, and booleans; nested arrays and objects are not supported. Values in `spec.template` may use the full Taskmigo Expression syntax and access declared props through `props.<name>`. See [Expressions](/versions/v0/manuals/expressions#use-context-values) for syntax and context.

Every node in `spec.template` must satisfy the selected component's contract.

## Use the Component in a Page

Import the Component by resource identity, then use its name as the node's `component` value. The node's `props` are validated against `spec.props`.

```yaml
apiVersion: v0
kind: page

metadata:
  name: dashboard

imports:
  - component/header

spec:
  meta:
    title: Dashboard
    description: Account overview

  body:
    - component: header
      props:
        title: Dashboard
        showDescription: false
```

Any number of Pages may import the same Component. Each Page supplies its own props while every use renders the same template.

An importing Page is invalid when it omits a required prop, supplies an undeclared prop, or provides a value that does not match the declared prop type.
